[build]
  command = "npm run build"
  publish = "dist"

# To prevent the @netlify/angular-runtime plugin from trying to run
[[plugins]]
  package = "@netlify/plugin-lighthouse" # Use a neutral plugin as a placeholder if needed, or simply leave empty to bypass auto-discovery
